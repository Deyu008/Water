name: water
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - PySide6>=6.6.0
      - qt-material>=2.14
      - pyqttoast>=1.2.0
      - pyinstaller>=6.0
      - pytest>=7.0
